#include <iostream>
using namespace std;

int addBinary(int a, int b)
{
    int ans = 0;
    int prevCarry = 0;

    while(a>0 && b>0)
    {
        if(a%2==0 )
    }
}

int32_t main()
{
    int a,b;
    cout<<"Enter two Binary Numbers for Sum"<<endl;
    cin>>a>>b;

    cout<<addBinary(a,b)<<endl;
}